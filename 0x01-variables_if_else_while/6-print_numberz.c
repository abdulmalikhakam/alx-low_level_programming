#include <stdio.h>
/**
 * main - Entry point
 * discription:'print all single digit numbers of base 10 starting from 0
 * Return: always 0
*/
int main(void)
{
	int n;

	for (n = 28; n < 38; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
